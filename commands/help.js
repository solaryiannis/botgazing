module.exports.run = (bot, message, args) => {
message.channel.send('**Info Commands**```help, m-help, servericon, serverinfo, usericon, userinfo```**Math Commands**```add, subtract, multiply, divide```**Other Commands**```bird, cat, coin, dog, roll, snap, tarot```');
if (message.guild.id === '450088547857465349') {
    message.channel.send("**Bunker Commands** ```kinkshame, mod-me, sad, say, thanosquote```");
}
};